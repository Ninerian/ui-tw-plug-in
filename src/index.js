import postcssjs from "postcss-js";
import plugin from 'tailwindcss/plugin';


const mainFunction = ({addBase, addComponents, addUtilities, config, postcss}) => {

}

export default plugin(mainFunction, {
    
})